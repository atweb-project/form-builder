export const DB_PROVIDER = 'DbConnectionToken';

export const USER_PROVIDER = 'UserModelToken';

export const FORM_PROVIDER = 'FormModelToken';

export const MONGO_URI = 'mongodb://localhost/form-builder';
